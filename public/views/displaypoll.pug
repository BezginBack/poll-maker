extends template.pug

block title
    title | Hoppala!
    
block content
    script(src='/controllers/pollLoadEvent.js')
    script(src='/controllers/chartObjects.js')
    div.pop-over
        div#copy copy to clipboard
    div.alertdiv
    if (username)
        div.data-name(data-name=username)
    else
        div.data-name
    div.holder
        div.header-poll-page(data-user=user data-title=polltitle)
            div
                label.text-info
            div
                p.text-muted
        div.options-option
        div.bar-canvas
                canvas#canvas-bar
        div.share-url
            div
                label.text-info
                p.text-muted
        div.footer-buttons
            div
                div.col-sm-2
                    a.btn
                div.col-sm-2
                    a.btn
                div.col-sm-2
            div.liner